# macOS
.DS_Store
.DS_Store?
._*

# Python
__pycache__/
*.pyc
*.pyo

# Debug / local logs
*.log

# Claude Code 本地配置（包含本机路径，不应提交）
.claude/
.aicli-pending/

# 由 install.sh 生成，包含用户路径和 API 配置，不应提交
# The actual run.sh lives in ~/Library/Application Support/aicli-cheatsheet/
# native-host/run.sh in this repo is just a placeholder/documentation

# API 密钥文件（install.sh 生成在安装目录，兜底防止误放入仓库）
credentials.env
